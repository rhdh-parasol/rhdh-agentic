{
  "name": "backstage-agent",
  "version": "0.1.0",
  "description": "Intent-based CLI for AI coding agents to interact with Backstage",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "backstage-agent": "./bin/backstage-agent"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "yaml": "^2.8.4"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
